version: "3.9"
services:
  honeyjar:
    build: .
    container_name: honeyjar
    restart: unless-stopped
    env_file: .env
    ports:
      - "22:22"
      - "23:23"
      - "80:80"
      - "445:445"
    volumes:
      - ./logs:/app/logs
